MODULE Program1;

VAR
  BUFFER:ARRAY[0..220] OF CHAR;
  X,Y,Z:INTEGER;
  C1:CHAR;

BEGIN
  READ(C1);
  WRITE(C1);

  X := 25;
  Y := 25;
  Z := X + Y;
  WRITE(Z);

  Z := X * 2;
  WRITE(Z)
END Program1.